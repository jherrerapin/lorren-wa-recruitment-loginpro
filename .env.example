PORT=3000
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/loginpro_contingency?schema=public
META_VERIFY_TOKEN=pon_este_mismo_valor_en_meta
META_ACCESS_TOKEN=pega_tu_access_token_permanente
META_PHONE_NUMBER_ID=pega_tu_phone_number_id
ADMIN_WHATSAPP_NUMBER=3052982551
APP_BASE_URL=https://tu-dominio-api.com
ADMIN_USER=reclutador
ADMIN_PASS=cambia-esta-clave-ya
DEV_USER=desarrollador
DEV_PASS=cambia-esta-clave-dev
DISPATCH_MODULE_URL=
R2_ACCOUNT_ID=tu_account_id
R2_ACCESS_KEY_ID=tu_access_key_id
R2_SECRET_ACCESS_KEY=tu_secret_access_key
R2_BUCKET=loginpro-recruitment-cv-contingency
R2_PUBLIC_BASE_URL=
R2_ENDPOINT=https://<accountid>.r2.cloudflarestorage.com

# OpenAI
# OPENAI_MODEL es el modelo principal y también el respaldo de las tareas especializadas.
# Terra prioriza calidad; para mayor volumen y menor costo puede usarse gpt-5.6-luna.
# Los overrides siguientes son opcionales y solo se necesitan si una tarea debe usar otro modelo.
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-5.6-terra
OPENAI_EXTRACTION_MODEL=
OPENAI_CV_MODEL=
OPENAI_ATTACHMENT_MODEL=
OPENAI_CONTEXTUAL_REPLY_MODEL=
OPENAI_SUPERVISOR_REPLY_MODEL=
OPENAI_ENABLED=true
OPENAI_TIMEOUT_MS=14000
FF_RESPONSES_EXTRACTOR=true
USE_CONVERSATION_ENGINE=true
OPENAI_MIN_FIELD_CONFIDENCE=0.7
OPENAI_MIN_INTENT_CONFIDENCE=0.7

# Sesiones
# SESSION_SECRET debe ser un string aleatorio largo y seguro en producción.
# Genera uno con: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
SESSION_SECRET=cambia-este-secreto
SESSION_COOKIE_NAME=loginpro.sid

# Store de sesiones
# El panel administrativo usa PostgreSQL como store de sesión en producción y desarrollo.

# WhatsApp de Despacho (whatsapp-web.js)
# En Railway este proceso debe ejecutarse como servicio persistente: Serverless/App Sleeping DESHABILITADO.
# Usa UNA sola réplica, porque dos Chromium no deben compartir el mismo perfil LocalAuth.
# Monta un volumen y deja que RAILWAY_VOLUME_MOUNT_PATH apunte a él.
DISPATCH_WWEB_AUTO_START=true
DISPATCH_WWEB_WATCHDOG_ENABLED=true
# Solo define esta ruta si está realmente dentro de un volumen persistente.
DISPATCH_WWEB_AUTH_PATH=
# Marca true únicamente cuando DISPATCH_WWEB_AUTH_PATH está montado en almacenamiento persistente.
# La pantalla de WhatsApp mostrará una advertencia si detecta almacenamiento efímero.
DISPATCH_WWEB_AUTH_PERSISTENT=false
DISPATCH_WA_RECONCILE_INTERVAL_MS=60000
DISPATCH_WA_RECONCILE_LIMIT=200
DISPATCH_WA_RECONCILE_MESSAGES_PER_CHAT=20
